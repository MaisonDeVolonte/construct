# docs/audits/2026-08-03-settings.md

## Settings Audit #1: 2026-08-03 20:29

### state
- all four scopes parse, and every rule carries a why in its scope doc
- every live probe was refused: credential read, ssh key, force push, token mask
- 17 checks passed against 1 error and 3 warnings
- the error is inherited from a wrapped sidecar, not from the stack itself
- all three warnings trace to the write embargo being lifted for this session

### findings
- **Wrapped** — `scopes.sh` reports 7 errors, each a deny naming an internal call no rule sees
- **Drift** — project carries 4 rules its template does not, all `AGENTS/**` policy denies
- **Drift** — user is short the same 4, from the same lift
- **Guard** — nothing denies writes to `AGENTS/settings` or `AGENTS/hooks`, so this is editable

### resolutions
- [ ] no action on the bypass class; `@gitfresh` already models the answer by handing back
- [ ] restore `Write/Edit(AGENTS/hooks/**)` and `Write/Edit(AGENTS/settings/**)` to project
- [ ] restore the same pair to `~/.claude/settings.json`
- [ ] rerun `AGENTS/settings/settingsaudit.sh` and confirm `guard` flips from warn to pass

### telemetry
```text
=== settingsaudit.sh sidecar ===
audit_file: docs/audits/2026-08-03-settings.md
audit_count: 1
next_audit: 2
timestamp: 2026-08-03 20:32
probes: run
passes: 17
errors: 1
warnings: 3
--- findings ---
PASS  coverage   local          every rule documented, nothing stale
PASS  coverage   managed        every rule documented, nothing stale
PASS  coverage   project        every rule documented, nothing stale
PASS  coverage   project        permissions.allow still mirrors user
PASS  coverage   project        permissions.ask still mirrors user
PASS  coverage   project        permissions.deny still contains all of user
PASS  coverage   user           every rule documented, nothing stale
WARN  drift      project        4 rules only in the template, 0 only installed
WARN  drift      user           4 rules only in the template, 0 only installed
WARN  guard      project        nothing denies writes to AGENTS/settings or hooks
PASS  parse      local          parses, 1 rules
PASS  parse      managed        parses, 0 rules
PASS  parse      project        parses, 191 rules
PASS  parse      user           parses, 187 rules
PASS  probe      credential-read  refused as configured
PASS  probe      force-push       the hook denies it, as the failover intends
PASS  probe      ssh-key-read     refused as configured
PASS  probe      token-mask       GH_TOKEN reads as a sentinel rather than the real value
PASS  scope      project        no credentials block, which this scope could not honor anyway
PASS  wrapped    permissions    0 errors
ERROR wrapped    scopes         7 errors; run AGENTS/settings/scopes.sh for the detail
--- needs a human (rules no script can judge) ---
- a deny naming a path nothing on this disk holds is insurance, not a finding
- the probes prove today's boundary; an upgrade can move it without any file changing
- an allow this calls broad may be right, since the deny floor is what carries the weight
- a drift finding is a defect only when the installed copy is the stale one
================================
```
